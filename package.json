{
  "name": "imageguide-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that audits every image on a page and shows the saving from a modern format.",
  "homepage": "https://www.imageguide.dev",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/verify.mjs && node --test \"test/*.test.js\"",
    "verify": "node scripts/verify.mjs",
    "icons": "bash scripts/make-icons.sh",
    "zip": "bash scripts/build-zip.sh"
  }
}
